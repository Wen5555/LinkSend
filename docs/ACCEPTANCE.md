# Acceptance Matrix

Published test prerelease: `v0.2.0`, protocol V1, Wails 3 `v3.0.0-beta.18`, tag/source `426d58b6ab62ab7213475007305c0a403955c00f`. Current source is `0.3.0` with the simplified pairing/persistent-inbox change recorded in PROGRESS; it has not been published. This matrix separates source implementation, automated evidence, and real-network validation. A loopback result is never counted as LAN or NAT acceptance; Pre-release does not mean production acceptance.

| Area | Implemented source | Automated/current evidence | Target validation |
|---|---|---|---|
| Identity and trust | Ed25519 identity, `ABCD-EFGH` single-use code, automatic post-pair key pinning, per-device auto-accept and revoke | code normalization/single-use, pin persistence/key-change rejection and automatic inbox tests pass | New weaker first-pairing model and auto-accept on two real devices |
| Signaling | SQLite group, HTTP/WSS auth, presence, session/generation ownership, stale-handler cleanup, simultaneous-initiation arbitration, `relay=false`, product version health field | Local real-WSS lifecycle/race tests pass; Hong Kong test main `0.2.0` passes complete→immediate complete and reject→immediate complete through public signaling | Additional cancel/failure/deadline public repetitions; automated coverage already PASS |
| ICE and QUIC | Pion ICE over quic-go-owned UDP socket, automatic eligible-interface selection preferring ordinary link MTU with explicit priority/exclusion override, on-link LAN evidence, address-change invalidation, TLS 1.3 pinning, real terminal flush and redacted structured evidence | Current Windows interface fixture selects physical Ethernet ahead of the abnormal-MTU TUN; real ICE+QUIC lifecycle/inbox/resume tests and race pass; prior physical Windows↔macOS LAN passes; independent Linux dual NAT with explicit UDP mapping passes 8 MiB while MASQUERADE-only returns `CHECK_TIMEOUT` | Repeat current source on physical Windows↔macOS LAN; public IPv6, physical network switch/sleep, and additional NAT types without fixed mapping |
| File protocol | Manifest, BLAKE3 chunks, safe root staging, verified missing-block requests, commit records and resumable checkpoints | 12 MiB pause/resume and damaged-block retransmit, 8 MiB full-Service restart resume, source/peer mismatch and partial-commit tests pass over real QUIC | Process kill during physical cross-device transfer and receiver-committed/sender-unconfirmed physical test |
| Task persistence | schema 2 revision-guarded snapshot + private recovery, corrupt-row quarantine, pre-migration v1 backup | migration/backup/readability, stale attempt, persistence-failure and restart recovery tests pass | Long-duration multi-process/profile misuse is unsupported; deployment profile migration/rollback drill pending |
| CLI | identity, pairing, devices, trust, revoke, diagnostics, send, receive, persisted `status`, explicit in-process `resume` | CLI tests plus shared app direct/recovery integration pass; two isolated Linux profiles transfer through independent mapped NATs | Physical cross-device NAT without fixed mapping; cross-process pause/cancel intentionally not claimed |
| Desktop | Wails 3 `v3.0.0-beta.18` persistent background inbox, incoming confirmation dialog, per-device no-confirm choice, Pause/Resume/Cancel bindings, revision-gated React task UI, and asynchronous native quit guard | 31-method/15-model bindings, desktop `GOWORK=off` test/vet/build, frontend typecheck/lint/8 tests/build and Windows production build pass; browser desktop/narrow layout inspected; isolated EXE remains alive after 4 seconds | Real native incoming dialog, auto-accept, file/directory dialogs, active-task close, macOS launch/quit and accessibility interactions |
| Packaging | Wails 3 Windows build/package and macOS dual-arch DMG scripts; explicit LinkSend NSIS metadata | main run `34600609161` built Release Windows ZIP + NSIS and arm64/amd64 DMGs from clean `426d58b`; local and GitHub digests match, Windows versions/NSIS pass, both runners report mount/bundle/arch/strict ad-hoc codesign PASS | Windows install/uninstall smoke, Developer ID signing, notarization, minimum-macOS runtime, release physical Mac launch and Intel Mac launch |
| Operations | Docker/Caddy/STUN-only examples, versioned healthz, redacted diagnostics | Hong Kong test main backup/deploy/health/public lifecycle PASS with rollback-ready old binary; compose and isolated coturn/HTTPS lab checks PASS | Actual rollback remains NOT_RUN because deployment succeeded; TURN Allocate rejection and certificate negative matrix remain |
| Race and static checks | No race-specific code bypass | local full test/vet/build/race pass; `426d58b` main core/desktop checks pass; terminal QUIC suite 50–100 repetitions and race 20 repetitions pass | Repeat real-network recovery on physical target OSes |

Current validation gaps are public IPv6, physical network switching/sleep, NAT types that need endpoint-independent mapping or relay, installer install/uninstall, signing/notarization, minimum-macOS runtime, and the native Windows/macOS interaction matrix beyond window creation/basic Windows close. Controlled independent dual NAT is now PASS only for the explicitly mapped positive case; MASQUERADE-only remains a product FAIL (`CHECK_TIMEOUT`, relay not implemented). Pause, restart recovery and verified missing-block byte resume are implemented and locally validated; this does not retroactively make earlier packages capable. Hong Kong test main now runs `0.2.0`; production-use claims remain out of scope.
