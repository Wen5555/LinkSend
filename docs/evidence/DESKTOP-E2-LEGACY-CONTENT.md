# E2 旧内容入口处理证据

传输首屏不再渲染文字、链接或图片手动创作器，只保留文件/文件夹、目标设备和发送动作。既有内容记录及接收后的受控预览、复制、打开和另存能力保留；快照清理仍保护活动任务、恢复、队列、草稿和收到的用户文件。

启动迁移继续把持久队列中的 `queued`、`waiting_peer`、`running` 状态统一改为 `needs_attention`/`RESTART_CONFIRMATION_REQUIRED`。因此旧内容队列仍在“待发送”可见，必须点“确认继续”，不会在升级或重启后自动续发。现有 workspace/queue/content 集成测试覆盖该恢复门槛；前端测试额外确认传输页不再出现“保存内容草稿”和“内容类型”。

本轮只完成 U4 的旧手动入口和迁移部分；默认关闭、双端授权的自动剪贴板属于 E4，未提前实现或宣称通过。

## cdb9577 审查修复

存储页新增旧版内容草稿列表，可取消草稿并释放保护引用；“管理已排队的旧版内容”直接打开传输→待发送，队列仍须点“确认继续”，不会自动续发。收到的用户文件不在删除范围。fixture 渲染与点击回执见[截图](assets/e2/e2-c1-legacy-content-960x640.png)。
