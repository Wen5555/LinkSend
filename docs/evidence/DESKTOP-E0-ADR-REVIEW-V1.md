# E0-ADR-review-v1 审查回复

后续状态：总控E0-safeio-close-v1已复审接受11c44df中的语义；具体API/schema/实现/兼容测试仍需E1/E4。下文保留提交该回复时的待审状态和检查事实。

2026-09-13。基于E0-A提交 `ec55db317b0b5b0dfa60c1a61219f28da9b401da` 的文档修订；状态**待总控复审，尚未冻结**。
已读取实际审查文件 `C:/Users/Wen/.codex/supervision/linksend-desktop-experience/pending-e0-adr-review.md`。其“尚无待审commit”描述的是较早工作树；现已有E0-A，不改写原审查记录。
该输入文件SHA256：`3a6b8dc1d3bee533f9582cff3555d8496fa76b0e10ad5a18695b533b1cf0da98`。

## 逐项处理

| 审查项 | 本次修订 | 关联TODO |
|---|---|---|
| R1 首帧/重传滞留不能重获TTL | 接收方预签发短时lease，绑定会话/双方身份与授权代际/方向/类型；接收方保存不可延长的单调deadline；事件只在取得lease并记录OS generation基线后产生，固定lease不能改绑续期 | E0-05、E4-02/03 |
| R1 长会话/断线/落板 | 同连接最多两个未到期lease供后续新事件，不续旧事件；断线/锁屏/代际变化清空；首帧、正文、解码、落板门闩均复核原deadline；包含睡眠的单调时钟来源须原生核实 | E4-01/03 |
| R2 双关系删除 | 持久推进peer级本机授权generation，使全部旧组/LAN/provisional grant失效；所有通道先查该屏障，禁止组失效后回落LAN；既有会话和迟到确认也重验 | E1-02/03/05 |
| R2 重新配对 | 新有效入组incarnation只建立新组配对；旧LAN、文件免确认和剪贴板不恢复。独立LAN需新同意，仅本机屏蔽仍须显式解除 | E1-01/02/03 |
| R3 序号/因果/提交 | origin_sequence用于本来源事件去重；Lamport独立按有效观察推进；真实重复复制相同正文仍新建事件；并行解码统一经原生提交owner核对OS generation与应用revision，旧检查不能由两个候选复用 | E4-01/03 |
| R4 原型真实边界 | Windows安装FAIL/激活NOT RUN保持为原生总体PARTIAL；Mac实际注册/系统服务激活有证据，但自定义回调/文件交接仍未通过；不将AX/签名缺口推断为永久不支持 | E0-03/04、E3-01/02/03 |

租约新鲜度已有明确因果证明（L≤E且C<L+TTL，因此C−E<TTL），不是“收到后再计时”。该证明不声称识别恶意授权发送者伪造的复制时间/正文来源；它约束正确实现的采集及传输事件。
应用内串行门闩与OS跨进程原子性也明确分开：Windows应在原生剪贴板所有权内检查/写入；macOS全系统CAS尚无已核实实现，不把应用锁当成全系统保证。

## 场景覆盖与验证状态

以下均已写入ADR及后续验收要求，**代码/自动测试/原生/网络本次全部NOT RUN**：

- 第一帧、整图、QUIC重传滞留超过lease期限；解码/候选排队跨deadline。
- 长会话新lease的新复制成功，旧事件不能换lease；断线重连旧nonce、睡眠跨期限与无lease时旧复制不补发。
- 双关系设备删除后WSS重连/LAN公告/旧控制/provisional grant不复活；合法新码只恢复新的配对关系。
- origin_sequence去重与Lamport因果推进独立；同文字真实新复制；两个远端同时完成解码，旧revision至多一次落板；本机新复制使旧候选失效。

本次仅修改ADR、PROTOCOL、SECURITY、ARCHITECTURE、PROGRESS、TODO和证据文档，无产品代码/wire/schema/安装包/主机状态变更。不重复跑不受影响的Go/前端/原生测试，不把此前通过当新增协议验证。
实际 `git diff --check` 与文档相对链接存在性检查退出0；暂存后再次检查完整本次diff及文件范围。原始两份用户材料保持不变。

## 仍须总控判断和继续执行

请复审上述语义后判定是否冻结；本任务没有自行宣布E0-05验收通过。E0-01/02现有准确包hash、Windows原生复现、三主机和香港控制面证据不变，尚缺准确两平台安装身份与完整物理桌面双向链路/双NAT等已列矩阵，不能因本次审查回复而整体标为完成。
Windows信任/安装权限与Mac签名/AX缺口不应阻止其他独立E0工作；Mac回调缺陷仍需技术定位，不能混称外部权限阻塞。U6最终要求保持真实文件授权、冷启动、交接和扩展退出后继续发送。
未push。跨任务发送工具当前仍不在可用工具列表，交接通过本修订提交、工程记录及任务回复完成，不声称已经工具送达总控。
