# Stage 2B — 香港正式入口与 Wails 3 候选（2026-09-09）

> 历史实测与候选记录：本页的旧服务 SHA、成员计数、`0.1.0` CI 包及 NOT_RUN 状态只描述 2026-09-09/10。香港测试主站现运行 `0.2.0`，新候选、回滚与剩余 FAIL 见 [DEPLOY-HK](DEPLOY-HK.md)、[PROGRESS](PROGRESS.md) 和 [ACCEPTANCE](ACCEPTANCE.md)。历史邀请前缀不应复用。

## 通过

- 香港 `hk-main`：Debian 12 x86_64，`https://linksend.oooai.de`，服务端 `/opt/linksend-lan-test/rendezvous` SHA256 `53445BE88943FBABDAA7AABFAF98E299AD9780B1084DAA764CF8804E48E5A338`。变更前 SQLite 在线备份 `/opt/linksend-lan-test/backups/20260909T133335Z-wails3/server.db`，`PRAGMA integrity_check` 为 `ok`；配置/二进制/证书副本同目录保留。
- 固定码 `orion123` 配置到现有组（组 ID 仅记录前缀 `73cde936…`）。`hk-win-wails3` 与 `hk-peer-wails3` 真实加入响应均 `admin=true`、同组；重复同一身份幂等。固定码入口保持开启，通用示例配置仍关闭。
- 动态邀请码 `fYJZA6…` 加入 `hk-dynamic-wails3`，响应 `admin=false`；再次消费返回 `AUTHENTICATION_FAILED`。
- 服务重启前后：健康接口均 `status=ok`、`protocol_version=1`、`transport=quic`、`relay=false`；重启后数据库同组计数为 8 成员、5 管理员、0 撤销。
- Windows 物理接口 `10.234.232.205` 上通过香港信令完成双向直连：
  - Windows→peer：1 MiB，任务 `4773432abda776c76882261f55c0b491`，源/目标 SHA256 `30E14955EBF1352266DC2FF8067E68104607E750ABB9D3B36582B8AF909FCB58`。
  - peer→Windows：2 MiB，任务 `cf38b13233b0214c7d309e28bf823d0f`，源/目标 SHA256 `5647F05EC18958947D32874EEB788FA396A05D0BAB7C1B71F112CEB7E9B31EEE`。
  - 两次均记录 TLS 1.3、ALPN `linksend/1`、host↔host、`relay=false`；文件字节未进入信令或 JavaScript IPC。

## 未运行 / 外部阻塞

- `BLOCKED_BY_EXTERNAL_ENV`：真实 Windows↔macOS Wails 3 原生窗口、Mac 红点/Cmd+Q、原生 consent/progress、两种 DMG 挂载和实机传输；当前主机为 Windows，无 macOS WebKit/Xcode/hdiutil。
- `NOT_RUN`：跨 NAT、IPv6、网络切换/睡眠唤醒、Docker 引擎/coturn 实际容器启动、生产 DNS/证书/防火墙改动（本轮未改这些记录）。
- 本记录是同一 Windows 主机物理网卡的 host↔host 证据，不扩展为跨 NAT 或跨平台结论。

## Wails 3 候选与 CI（2026-09-10）

- 任务分支 `codex/wails3-hk-dmg-20260909`，候选 commit `555c3190c4c5f13a52eebe9b9a8f6208abb7ee7d`。
- GitHub Actions [run 34387753173](https://github.com/Wen5555/LinkSend/actions/runs/34387753173) 的
  `windows-amd64`、`macos-arm64`、`macos-amd64` 均为 `success`；Windows ZIP 和两种真实 DMG
  均已下载并通过 ZIP/DMG 结构、SHA256SUMS、bundle ID、Go 架构与 Wails 依赖核验。
- 包级 SHA256：Windows ZIP `1c43b7576118c5480d139bd691a1c84b8c3d09f67d96f8815c1877c4055f8f80`；
  macOS arm64 DMG `5ff0217fab3b257999eae710f29bc76f08979b37100b2f1b4347546a6d3645a2`；
  macOS amd64 DMG `653c2ceb098c5d81bc4c6f0a351cb20b446772c206888463aec7b34efff73819`。
- Windows ZIP 包含 `LinkSend.exe` 与 `README-WINDOWS-TEST.txt`；DMG 包含 `LinkSend.app` 与
  `README-MACOS-TEST.txt`。当前 Windows 主机无法做 DMG 挂载、Mac 原生窗口、红点/Cmd+Q 或
  Mac 实机传输，以上保持 `NOT_RUN`/`BLOCKED_BY_EXTERNAL_ENV`。
