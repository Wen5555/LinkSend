listen = "127.0.0.1:8787"
database = "data/server.db"
allow_insecure_loopback = true
allow_loopback_candidates = true
tls_cert = ""
tls_key = ""
# Repeatable local UI/e2e pairing only. Never copy this setting to a public server.
test_pairing_code = "orion123"
